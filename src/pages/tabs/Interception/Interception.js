import React from 'react';
import PropTypes from 'prop-types';

const Interception = props => {
    return (
        <div>

        </div>
    );
};

Interception.propTypes = {

};

export default Interception;